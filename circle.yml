machine:
  timezone: UTC
  java:
    version: oraclejdk8


dependencies:
  # Cache the resolution-cache and build streams to speed things up
  cache_directories:
    - "$HOME/.m2"
    - "$HOME/.ivy2/cache"
    - "$HOME/.sbt/boot/"
  # Compile all sources
  override:
    - sbt test:compile

test:
  pre:
    - git config --global user.name "CircleCI"
    - git config --global user.email "riomus@gmail.com"
  override:
    - sbt clean sparkling-graph-loaders/test:test
#  post:
    #- bash <(curl -s https://codecov.io/bash)
    #- sbt ghpages-push-site
    #- sbt publish